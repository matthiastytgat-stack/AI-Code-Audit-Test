version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: echo "install dependencies here"
      - run:
          name: Build
          command: echo "build steps here"

  test:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run:
          name: Run tests
          command: echo "test steps here"

workflows:
  build-and-test:
    jobs:
      - build
      - test:
          requires:
            - build